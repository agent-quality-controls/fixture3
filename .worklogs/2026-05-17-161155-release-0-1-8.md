Summary
- Fixed metadata drift blocking check: stale approved fixture or manifest hashes no longer prevent writing current received output and metadata.
- Rewrote the hash-drift behavior fixture expectation and bumped fixture3 to 0.1.8 for release.

Decisions made
- Removed metadata hash validation from the received-write path because fixture and manifest edits are part of normal approval review.
- Kept approved metadata replacement in approve: current received metadata becomes approved metadata after approval.
- Verified the behavior through the existing hash-drift self fixture instead of adding Rust tests.

Key files for context
- apps/fixtures/crates/fixture3/src/storage.rs
- apps/fixtures/crates/fixture3/src/metadata.rs
- scripts/self-check-harness.py
- behavior/approved/self/approved.normalized.json
- .plans/2026-05-17-160827-nonblocking-metadata-drift.md

Verification
- cargo run --manifest-path apps/fixtures/Cargo.toml -p fixture3-cli -- check --suite self
- scripts/verify-all.sh
- python3 scripts/verify-rust-workspaces.py --g3rs
- g3rs validate repo
- git diff --check
- cargo publish --dry-run --allow-dirty --manifest-path apps/fixture3-install/crates/fixture3-install/Cargo.toml

Next steps
- Tag v0.1.8, wait for binary-release assets, publish fixture3 v0.1.8 to crates.io, and verify cargo-binstall installs 0.1.8.
