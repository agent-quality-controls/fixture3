# fixture3 Reducer Plan

## Goal

Add `fixture3 reduce` as a deterministic reducer for oversized copied-project fixtures.

The reducer shrinks fixture content while preserving a suite-owned behavior contract. It produces machine-readable proof metadata that shows every reducer pass, every accepted removal, and every rejected removal.

The guarantee is:

- No built-in reducer pass found removable content while preserving the configured contract.

The guarantee is not:

- global mathematical minimality
- minimality across alternate fixture designs
- semantic equivalence outside the configured contract

## Current Status

This plan is not implementation-ready.

Implementation is blocked until the research gate below is completed and this plan is rewritten from the research results.

The current plan still contains provisional decisions that may be replaced by the research gate.

## Open Questions

### Reducer Algorithm

- OQ-REDUCE-01: Should the filetree and TOML reducers use Zeller and Hildebrandt's `ddmin` delta debugging algorithm instead of the current greedy one-candidate loop?
- OQ-REDUCE-02: What guarantee can fixture3 truthfully make after `ddmin`? The candidate wording is "1-minimal within the configured candidate set and oracle contract", not "globally minimal".
- OQ-REDUCE-03: Does a maintained Rust `ddmin` crate exist with at least 100 GitHub stars and a default-branch commit no older than 2025-05-15?
- OQ-REDUCE-04: If no acceptable Rust `ddmin` crate exists, what is the smallest correct in-project implementation, and which paper or reference implementation does each step cite?
- OQ-REDUCE-05: Should `picire` influence the design as a parallel delta-debugging reference even though it fails the star threshold?
- OQ-REDUCE-06: Should C-Reduce influence reducer architecture even though it fails the recency threshold?
- OQ-REDUCE-07: Which candidate granularities are mandatory for a useful first implementation: files, directories, TOML table entries, TOML array values, Rust syntax nodes, or package-layout stubs?
- OQ-REDUCE-08: How are unresolved trials handled? The oracle cannot be binary if timeouts, invalid intermediate states, command crashes, or nondeterminism are possible.

### Oracle and Mutation Model

- OQ-ORACLE-01: Should every candidate trial run in a temporary workspace instead of mutating the real fixture root?
- OQ-ORACLE-02: Should normal mode apply only the final reduced tree to the real fixture after the final oracle passes?
- OQ-ORACLE-03: What exact tri-state oracle vocabulary is used: `interesting`, `not_interesting`, and `unresolved`, or different names?
- OQ-ORACLE-04: What retry and timeout policy is required before a trial is marked unresolved?
- OQ-ORACLE-05: What environment variables, current working directory, stdin, stdout limits, stderr limits, and filesystem limits are part of the oracle contract?
- OQ-ORACLE-06: What cache key is valid for oracle results: candidate hash, command hash, manifest contract hash, environment hash, and tool version hash?

### Spec Validation and Spec-Driven Development

- OQ-SPEC-01: Is fixture3 only an approval-testing tool, or should it also define a conformance-testing model for spec-driven development?
- OQ-SPEC-02: Which existing discipline should fixture3 borrow from: conformance testing, model-based testing, property-based testing, metamorphic testing, contract testing, executable specifications, approval testing, mutation testing, or a combination?
- OQ-SPEC-03: What is the machine-checkable source of truth: prose plan, manifest, fixture input, expected golden output, contract block, or an explicit spec file?
- OQ-SPEC-04: How does fixture3 prove that a behavior contract is not too weak? Possible answers include mutation testing against contracts, negative fixtures, required row IDs, coverage over spec IDs, or adversarial fixture reduction.
- OQ-SPEC-05: How are plan requirements mapped to fixture contracts so an agent cannot implement behavior without a corresponding observable fixture row?
- OQ-SPEC-06: Should every behavior row carry a stable spec ID, feature ID, and layer ID?
- OQ-SPEC-07: Should fixture3 require a verifier manifest that maps each spec ID to at least one fixture and at least one approved output row?
- OQ-SPEC-08: Can existing libraries provide spec validation primitives, or is this mostly a methodology plus manifest/verifier design?

### Contract Semantics

- OQ-CONTRACT-01: Should JSON row matching require a JSON Pointer root such as `/rows` instead of searching anywhere in the stdout JSON tree?
- OQ-CONTRACT-02: Should fixture3 support JSON Schema for output shape validation?
- OQ-CONTRACT-03: Should contracts support exact rows, forbidden rows, required counts, forbidden counts, sortedness, and stable IDs?
- OQ-CONTRACT-04: Should text output contracts be line-based with exact lines instead of substring exclusion?
- OQ-CONTRACT-05: What is the minimal contract language that prevents weak or accidental reducer success without becoming a second test framework?

### Dependency Policy

- OQ-DEPS-01: For every mentioned dependency or reference project, verify GitHub stars, default-branch latest commit date, license, maintenance state, and whether it is a dependency candidate or only a design reference.
- OQ-DEPS-02: Do not add a dependency that has fewer than 100 GitHub stars or no default-branch commit since 2025-05-15 unless the plan records a specific exception approved later.
- OQ-DEPS-03: Do not treat an installed local tool as relevant. Installed tools can be removed or replaced.

## Research Gate Task List

No reducer implementation may start until all tasks in this section are completed.

- TASK-REDUCE-01: Read and summarize Zeller and Hildebrandt, "Simplifying and Isolating Failure-Inducing Input", IEEE TSE 2002. Output: exact `ddmin` algorithm contract, complexity, and guarantee wording for fixture3.
- TASK-REDUCE-02: Read and summarize Hierarchical Delta Debugging literature. Output: whether fixture3 should use HDD concepts for TOML and Rust AST reduction, and what is still delegated to `treereduce`.
- TASK-REDUCE-03: Search Rust crates and GitHub for `ddmin` or delta-debugging libraries. Output: dependency table with stars, latest default-branch commit date, license, API fit, and accept/reject decision.
- TASK-REDUCE-04: Inspect `treereduce` API and examples. Output: exact integration model for reducing Rust source in a temporary workspace through fixture3's oracle.
- TASK-REDUCE-05: Inspect `picire`. Output: design lessons only, because current verification shows it fails the 100-star threshold.
- TASK-REDUCE-06: Inspect C-Reduce. Output: design lessons only, because current verification shows it fails the one-year recency threshold.
- TASK-ORACLE-01: Design the tri-state oracle. Output: manifest fields, result enum, timeout/retry rules, stdout/stderr limits, environment policy, and cache key.
- TASK-ORACLE-02: Design the mutation model. Output: exact temp-workspace lifecycle, final apply step, crash behavior, proof paths, and dry-run behavior.
- TASK-SPEC-01: Research conformance testing, model-based testing, property-based testing, metamorphic testing, contract testing, executable specifications, approval testing, and mutation testing. Output: which ideas fixture3 adopts and which it rejects.
- TASK-SPEC-02: Design spec-to-fixture traceability. Output: manifest shape for spec IDs, feature IDs, layer IDs, required rows, negative rows, and verifier checks.
- TASK-SPEC-03: Design weak-contract detection. Output: concrete checks that fail when a fixture can pass while removing the behavior it was meant to protect.
- TASK-CONTRACT-01: Design JSON contract semantics. Output: JSON Pointer row root, exact/partial row rules, counts, forbidden rows, schema validation decision, and error messages.
- TASK-DEPS-01: Re-verify every dependency candidate immediately before implementation. Output: committed dependency audit with command output dates.
- TASK-PLAN-01: Rewrite this plan after TASK-REDUCE, TASK-ORACLE, TASK-SPEC, TASK-CONTRACT, and TASK-DEPS are complete. Output: implementation-ready plan with no unresolved open questions.

## Verified Dependency and Reference Snapshot

Checked on 2026-05-15 with `gh repo view`, `gh api repos/<repo>/commits?per_page=1`, `cargo search`, and `cargo info`.

- `treereduce`
  - Repo: `https://github.com/langston-barrett/treereduce`
  - Stars: 113
  - Latest default-branch commit: 2026-05-12
  - License: MIT
  - Status: passes threshold
  - Role: dependency candidate for syntax-aware Rust source reduction

- `tree-sitter-rust`
  - Repo: `https://github.com/tree-sitter/tree-sitter-rust`
  - Stars: 498
  - Latest default-branch commit: 2026-03-27
  - License: MIT
  - Status: passes threshold
  - Role: dependency candidate through `treereduce`

- `toml_edit`
  - Repo: `https://github.com/toml-rs/toml`
  - Stars: 1038
  - Latest default-branch commit: 2026-05-14
  - License: Apache-2.0 in GitHub metadata; crate is MIT OR Apache-2.0
  - Status: passes threshold
  - Role: dependency candidate for semantic TOML edits

- `walkdir`
  - Repo: `https://github.com/BurntSushi/walkdir`
  - Stars: 1501
  - Latest default-branch commit: 2024-12-31
  - License: Unlicense in GitHub metadata; crate is Unlicense OR MIT
  - Status: fails recency threshold
  - Role: provisional dependency candidate only if a maintained replacement is not better

- `ignore`
  - Repo: `https://github.com/BurntSushi/ripgrep`
  - Stars: 63787
  - Latest default-branch commit: 2026-02-27
  - License: Unlicense in GitHub metadata; crate is Unlicense OR MIT
  - Status: passes threshold
  - Role: rejected for reducer traversal because ignored files must remain visible

- `tempfile`
  - Repo: `https://github.com/Stebalien/tempfile`
  - Stars: 1428
  - Latest default-branch commit: 2026-05-07
  - License: Apache-2.0 in GitHub metadata; crate is MIT OR Apache-2.0
  - Status: passes threshold
  - Role: dependency candidate for temporary workspaces

- `similar`
  - Repo: `https://github.com/mitsuhiko/similar`
  - Stars: 1266
  - Latest default-branch commit: 2026-04-11
  - License: Apache-2.0
  - Status: passes threshold
  - Role: optional dependency candidate for human-readable reducer reports

- `sha2`
  - Repo: `https://github.com/RustCrypto/hashes`
  - Stars: 2214
  - Latest default-branch commit: 2026-05-13
  - License: crate is MIT OR Apache-2.0
  - Status: passes threshold
  - Role: existing dependency for proof and tree hashes

- `picire`
  - Repo: `https://github.com/renatahodovan/picire`
  - Stars: 55
  - Latest default-branch commit: 2025-11-23
  - License: Other
  - Status: fails star threshold
  - Role: design reference only, not a dependency candidate

- C-Reduce
  - Repo: `https://github.com/csmith-project/creduce`
  - Stars: 1659
  - Latest default-branch commit: 2024-06-01
  - License: Other
  - Status: fails recency threshold
  - Role: design reference only, not a dependency candidate

- `evaporust`
  - Repo: `https://github.com/ibabushkin/evaporust`
  - Stars: 0
  - Latest default-branch commit: 2019-09-24
  - License: crate is MIT OR Apache-2.0
  - Status: fails star and recency thresholds
  - Role: rejected

- `fs_extra`
  - Repo: `https://github.com/webdesus/fs_extra`
  - Stars: 334
  - Latest default-branch commit: 2023-02-03
  - License: MIT
  - Status: fails recency threshold
  - Role: rejected

- `pathdiff`
  - Repo: `https://github.com/Manishearth/pathdiff`
  - Stars: 66
  - Latest default-branch commit: 2025-02-25
  - License: Apache-2.0
  - Status: fails star and recency thresholds
  - Role: rejected

- Rust `ddmin` crate
  - `cargo search ddmin --limit 10`: no relevant reducer crate found
  - `gh search repos 'ddmin rust' --limit 10`: no relevant repo found
  - `gh search repos 'delta debugging rust' --limit 10`: no relevant repo found
  - Status: no acceptable Rust dependency found yet
  - Role: algorithm to cite and possibly implement, not a verified crate

## Current Research Stage: DDMin

Standalone algorithm plan:

- `.plans/2026-05-15-145518-ddmin-package.md`

Checked on 2026-05-15 with:

- `cargo search ddmin --limit 50`
- `cargo search "delta debugging" --limit 50`
- `cargo search "test case reduction" --limit 50`
- `gh search repos 'ddmin language:Rust' --limit 50`
- `gh search repos 'delta debugging Rust' --limit 100`
- `gh search repos 'test case reduction Rust' --limit 100`
- `gh search repos 'ddmin' --limit 100`
- `gh search repos 'delta-debugging' --limit 100`
- targeted reads of candidate repository metadata and source files

Findings:

- No acceptable maintained Rust crate exists for standalone generic `ddmin`.
- `ddmin` is an algorithm from Zeller and Hildebrandt, not a package fixture3 can currently depend on.
- `treereduce` remains useful for syntax-aware source reduction, but it is not a generic fixture-candidate reducer.
- Perses has richer reducer algorithms, but GPL-3.0 makes it unsuitable as a normal fixture3 dependency or bundled backend.
- The fixture3-owned custom code should be limited to a small generic `ddmin` core plus domain-specific candidate generation, materialization, oracle evaluation, and proof metadata.
- Advanced Perses-style algorithms such as ProbDD, WDD, CDD, token slicing, tree slicing, T-Rec, Vulcan, and formatter hooks are not required for the first fixture-tree reducer.
- DFS and BFS are candidate traversal or grouping strategies, not replacements for `ddmin`.
- For fixture3, BFS-style grouping can create coarse groups such as directories or config sections before `ddmin` runs over those groups.

Rust candidates reviewed:

- `bkushigian/delta-debug`
  - Repo: `https://github.com/bkushigian/delta-debug`
  - Stars: 0
  - Latest default-branch commit: 2020-07-13
  - License: none found
  - Crates.io: not published as `delta-debug`
  - Source shape: small generic `minimize<T, F>(Vec<T>, oracle) -> Vec<T>`
  - Decision: do not depend; useful only as a reference shape

- `jubnzv/dd`
  - Repo: `https://github.com/jubnzv/dd`
  - Stars: 1
  - Latest default-branch commit: 2022-11-19
  - License: none found
  - Source shape: Lua/tree-sitter reducer with a `ddmin` function tied to syntax nodes and pass objects
  - Decision: reject as dependency; weak reference only

- `JacobSalvi/delta-debugging`
  - Repo: `https://github.com/JacobSalvi/delta-debugging`
  - Stars: 0
  - Latest default-branch commit: 2024-10-05
  - License: none found
  - Source shape: string demo with regex oracle
  - Decision: reject

- `britesma/delta-debugging`
  - Repo: `https://github.com/britesma/delta-debugging`
  - Stars: 0
  - Latest default-branch commit: 2022-10-16
  - License: GPL-3.0
  - Source shape: string demo, not generic
  - Decision: reject

- `ibabushkin/evaporust`
  - Repo: `https://github.com/ibabushkin/evaporust`
  - Stars: 0
  - Latest default-branch commit: 2019-09-24
  - License: crate is MIT OR Apache-2.0
  - Source shape: published crate is effectively a placeholder
  - Decision: reject completely

Decision for the next plan rewrite:

- Implement a generic in-project `ddmin` core only if TASK-REDUCE-01 confirms the exact algorithm contract from the paper.
- Keep the `ddmin` core independent from filesystem, TOML, Rust source, process execution, and proof code.
- Use fixture3 behavior fixtures instead of Rust unit tests to prove the reducer edge cases.
- Do not implement ProbDD, WDD, CDD, HDD, or Perses-style slicers in the first implementation.
- Do not use Perses as a dependency or bundled backend.
- Use `treereduce` only for optional source-file reduction after the fixture-candidate reducer works.

`ddmin` behavior fixtures must cover:

- baseline input satisfies the oracle
- empty candidate set
- one required candidate
- multiple required candidates
- duplicate candidates
- order-preserving subsequences
- non-monotonic oracle behavior
- no removable candidate at fixpoint
- unresolved oracle trials are never accepted as successful reductions

## Provisional Decisions

These decisions are recorded context, not implementation authority.

Agents must complete the research gate and rewrite this section before implementing `fixture3 reduce`.

- `reduce` mutates the fixture in place by default.
- `--dry-run` runs the same reducer against backups and restores all accepted edits before exit.
- `--dry-run` prints the proof report to stdout and does not write the proof file.
- Normal mode writes proof metadata to `reduce.proof`.
- `reduce.proof` lives inside the fixture root by default as `.fixture3-reduce.json`.
- `reduce` accepts exactly one `--suite`.
- `reduce` does not accept `--all`, `--tag`, or `--feature`.
- `reduce` does not accept `--pass`.
- `reduce` exits `0` after a completed reduction with a passing final contract.
- `reduce` exits `2` for setup errors, generated-directory violations, baseline contract failure, oracle command failure, or final contract failure.
- `reduce` does not use exit code `1`.
- `walkdir` is the filetree traversal dependency.
- `ignore` is not used for the reducer.
- Guardrail3 `g3-workspace-crawl` is not used for the reducer.
- `toml_edit` is the TOML reducer dependency.
- `treereduce` and `tree-sitter-rust` are the Rust source reducer dependencies.
- `treereduce` runs with one job in the first implementation.
- Symlinks under `reduce.root` are rejected.
- Generated folders are rejected, not reduced.
- Source reduction is controlled by explicit manifest globs.
- Reducer pass order is fixed in code.
- Contract matching uses JSON row matching for stdout and text exclusion for stderr.
- Substring matching is not used for stdout contract requirements.
- The verifier fixture lives under `examples/fake-project/reducer-project`.

## Problem

Copied-project fixtures become too large because they preserve unrelated project content.

The reducer removes:

- unrelated files
- unused Cargo dependencies
- unused TOML keys
- unused Rust source items
- empty directories left after accepted removals

The reducer rejects:

- generated directories
- symlinks
- unsupported file types during reducer-owned copy operations
- suites without a reducer contract

## Dependency Decisions

This section is superseded by "Verified Dependency and Reference Snapshot" and "Research Gate Task List".

Agents must not add dependencies from this section until TASK-DEPS-01 re-verifies them and TASK-PLAN-01 rewrites the implementation plan.

Dependencies already checked on 2026-05-14 and 2026-05-15.

- `walkdir`
  - Repo: `https://github.com/BurntSushi/walkdir`
  - Stars: 1501
  - Last push: 2024-12-31
  - License: Unlicense OR MIT
  - Use: physical fixture tree traversal

- `toml_edit`
  - Repo: `https://github.com/toml-rs/toml`
  - Stars: 1038
  - Last push: 2026-05-14
  - License: MIT OR Apache-2.0
  - Use: semantic TOML and `Cargo.toml` edits

- `tempfile`
  - Repo: `https://github.com/Stebalien/tempfile`
  - Stars: 1429
  - Last push: 2026-05-07
  - License: MIT OR Apache-2.0
  - Use: backup files and temporary reducer state

- `treereduce`
  - Repo: `https://github.com/langston-barrett/treereduce`
  - Stars: 113
  - Last push: 2026-05-12
  - License: MIT
  - Use: syntax-aware Rust source reduction

- `tree-sitter-rust`
  - Repo: `https://github.com/tree-sitter/tree-sitter-rust`
  - Stars: 498
  - Last push: 2026-03-27
  - License: MIT
  - Use: Rust parser for `treereduce`

- `similar`
  - Repo: `https://github.com/mitsuhiko/similar`
  - Stars: 1267
  - Last push: 2026-04-11
  - License: Apache-2.0
  - Use: reducer report diffs

- `sha2`
  - Already present in `crates/fixture3/Cargo.toml`
  - Use: tree and proof hashes

Rejected:

- `ignore`
  - Reason: reducer traversal must see ignored files and generated folders.

- Guardrail3 `g3-workspace-crawl`
  - Reason: it is an ingestion snapshot with gitignore-aware filtering and recovery semantics.

- `fs_extra`
  - Reason: last push is older than one year.

- `pathdiff`
  - Reason: below 100 stars and last push is older than one year.

- `evaporust`
  - Reason: published crate is effectively a placeholder.

## Command Surface

Supported commands:

```bash
fixture3 reduce --suite <suite>
fixture3 reduce --suite <suite> --dry-run
fixture3 reduce --suite <suite> --json
```

Rejected command shapes:

```bash
fixture3 reduce --all
fixture3 reduce --tag <tag>
fixture3 reduce --feature <feature>
fixture3 reduce --suite <suite> --pass filetree
fixture3 reduce --suite <suite> --proof <path>
fixture3 reduce --suite <suite> --write
```

Reason:

- mutation across several suites is not part of the first reducer contract
- individual pass execution exposes implementation details and creates partial states
- proof path belongs in the manifest
- normal mode already writes

## Manifest Shape

Each reducible suite contains a `reduce` block.

```yaml
suites:
  copied-rust-project:
    fixtures:
      - "examples/fake-project/reducer-project/input.json"
    command:
      argv:
        - "bash"
        - "examples/fake-project/scripts/replay-reducer-project.sh"
        - "{fixtures}"
      ok_exit_codes:
        - 0
    output:
      format: "json"
    storage:
      approved_dir: "examples/fake-project/behavior/approved/reducer-project"
      received_dir: ".fixture3/reducer-project"
      diff_dir: ".fixture3/reducer-project"
    reduce:
      root: "examples/fake-project/reducer-project/project"
      proof: "examples/fake-project/reducer-project/project/.fixture3-reduce.json"
      protected:
        - "Cargo.lock"
      generated_dirs:
        - "target"
        - ".git"
        - ".cargo-target"
        - "node_modules"
        - "dist"
        - ".fixture3"
        - ".goldencheck"
      source:
        rust:
          include:
            - "src/**/*.rs"
          exclude:
            - "src/generated/**/*.rs"
      contract:
        exit_code: 0
        stdout:
          format: "json"
          require_rows:
            - rule: "workspace_root"
              severity: "error"
          forbid_rows:
            - rule: "unexpected_generated_state"
        stderr:
          forbid_text:
            - "panicked at"
```

Manifest rules:

- `reduce.root` is required.
- `reduce.proof` is required.
- `reduce.contract` is required.
- `reduce.source.rust.include` is required.
- `reduce.generated_dirs` uses the default list shown above when absent.
- `reduce.protected` uses an empty list when absent.
- All reducer paths are relative to the manifest directory.
- `reduce.proof` must be under `reduce.root`.
- `reduce.root` must not be the repository root.

## Contract Semantics

The oracle runs the suite command and evaluates the reducer contract.

Exit-code contract:

- actual command exit code must equal `reduce.contract.exit_code`

Stdout contract:

- stdout must parse as JSON
- `stdout.require_rows` contains partial JSON objects
- each required row must match at least one object anywhere in the stdout JSON tree
- a row matches when every field in the required row exists with the same JSON value
- `stdout.forbid_rows` contains partial JSON objects
- forbidden rows must match zero objects in the stdout JSON tree

Stderr contract:

- each `stderr.forbid_text` string must be absent from stderr

Unsupported contract forms:

- stdout substring requirements
- JSONPath expressions
- count ranges
- regex matching

Reason:

- partial JSON row matching is enough for fixture behavior rows
- JSONPath adds dependency and selector semantics before the first reducer needs them
- stdout substring matching is too weak for a reducer oracle

## Reducer Architecture

Files to add:

```text
crates/fixture3/src/reducer.rs
crates/fixture3/src/reducer/config.rs
crates/fixture3/src/reducer/oracle.rs
crates/fixture3/src/reducer/filetree.rs
crates/fixture3/src/reducer/toml.rs
crates/fixture3/src/reducer/source.rs
crates/fixture3/src/reducer/proof.rs
```

Files to update:

```text
crates/fixture3/src/app.rs
crates/fixture3/src/args.rs
crates/fixture3/src/manifest.rs
crates/fixture3/src/fs.rs
crates/fixture3/Cargo.toml
README.md
fixture3.yaml
scripts/verify-all.sh
```

Boundary rules:

- `app.rs` only routes the command.
- `manifest.rs` only owns manifest parsing and manifest types.
- `reducer/config.rs` converts manifest types into reducer runtime config.
- `reducer/oracle.rs` owns command execution and contract evaluation.
- `reducer/filetree.rs` owns file and directory candidates.
- `reducer/toml.rs` owns TOML candidates.
- `reducer/source.rs` owns `treereduce` integration.
- `reducer/proof.rs` owns proof structs and serialization.
- `fs.rs` owns filesystem helper functions used by reducer modules.

## Execution Flow

`fixture3 reduce --suite copied-rust-project` runs this sequence:

1. Load manifest.
2. Resolve exactly one suite.
3. Convert `suite.reduce` into reducer config.
4. Reject missing reducer config with exit `2`.
5. Reject `reduce.root` outside the repository with exit `2`.
6. Reject `reduce.root` equal to the repository root with exit `2`.
7. Run baseline oracle.
8. Reject baseline contract failure with exit `2`.
9. Compute `baseline_hash`.
10. Run `generated-dirs` pass.
11. Run `filetree` pass.
12. Run `toml` pass.
13. Run `treereduce-rust` pass.
14. Remove empty directories.
15. Run final oracle.
16. Reject final contract failure with exit `2`.
17. Compute `final_hash`.
18. Write proof file in normal mode.
19. Print proof JSON in `--json` mode.
20. Print short text summary in normal text mode.

Pass order:

```text
generated-dirs -> filetree -> toml -> treereduce-rust -> empty-dirs
```

## Mutation Model

Normal mode:

- candidate edits are applied to the real fixture root
- every candidate starts with a backup
- accepted edits keep the modified fixture
- rejected edits restore from backup
- final proof is written to `reduce.proof`

Dry-run mode:

- candidate edits are applied to the real fixture root
- every accepted edit is recorded
- every accepted edit is restored before exit
- rejected edits restore immediately
- proof JSON is printed to stdout
- proof file is not written
- final filesystem state equals the state before the command started

Crash recovery:

- backups are stored under `.fixture3/reduce-backups/<suite>/<run-id>`
- backups are deleted after successful normal-mode completion
- dry-run deletes backups after successful restoration
- startup detects existing backup directories and exits `2` with the backup path
- no automatic recovery runs in the first implementation

## Filetree Pass

Traversal:

- use `walkdir`
- follow symlinks: false
- include hidden files
- include gitignored files
- sort candidates lexicographically by repository-relative path
- include regular files only
- reject symlinks
- reject special files

Candidate rules:

- skip paths in `reduce.protected`
- skip `reduce.proof`
- skip suite storage paths
- skip `.fixture3/reduce-backups`
- skip directories listed in `reduce.generated_dirs` after the generated-dir pass has checked them

Algorithm:

1. Build sorted regular-file candidate list under `reduce.root`.
2. Move candidate file to backup.
3. Run oracle.
4. Keep deletion when oracle passes.
5. Restore file when oracle fails.
6. Repeat until one complete pass removes zero files.
7. Remove empty directories under `reduce.root`.
8. Record tried, removed, and kept paths.

## Generated Directory Pass

Generated directories are rejected before any reducer mutates content.

Default generated directory names:

- `target`
- `.git`
- `.cargo-target`
- `node_modules`
- `dist`
- `.fixture3`
- `.goldencheck`

Algorithm:

1. Walk `reduce.root` with `walkdir`.
2. Collect directories whose final path component is in `reduce.generated_dirs`.
3. Exit `2` when the collection is non-empty.
4. Record checked directory names in proof when the collection is empty.

This pass does not delete generated directories.

## TOML Pass

Files:

- every `Cargo.toml` under `reduce.root`
- every `*.toml` under `reduce.root` not listed in `reduce.protected`

Dependency:

- `toml_edit`

Candidate types:

- remove one `[dependencies]` entry
- remove one `[dev-dependencies]` entry
- remove one `[build-dependencies]` entry
- remove one `[features]` entry
- remove one `[workspace.dependencies]` entry
- remove one `[workspace.members]` value
- remove one package metadata key under `[package.metadata]`

Algorithm:

1. Parse TOML with `toml_edit`.
2. Generate candidates in the order listed above.
3. Write one candidate edit.
4. Run oracle.
5. Keep edit when oracle passes.
6. Restore file when oracle fails.
7. Repeat until one complete pass removes zero TOML candidates.
8. Record tried, removed, and kept TOML paths.

Invalid TOML after an edit is a reducer bug and exits `2`.

## Rust Source Pass

Files:

- Rust files matched by `reduce.source.rust.include`
- protected paths are excluded
- paths matched by `reduce.source.rust.exclude` are excluded

Dependencies:

- `treereduce`
- `tree-sitter-rust`

Runtime settings:

- `jobs = 1`
- `min_reduction = 2`
- `passes = 2`
- `delete_non_optional = false`
- replacements: `type_identifier -> ()`

Algorithm:

1. Build sorted Rust file list from include and exclude globs.
2. Parse the current file with `tree-sitter-rust`.
3. Skip files with parse errors and record them as skipped.
4. Run `treereduce` with a fixture3 `Check` adapter.
5. For every candidate source from `treereduce`, write the candidate to the real file path.
6. Run oracle.
7. Return interesting when oracle passes.
8. Restore previous source when oracle fails.
9. Keep the final reduced source when `treereduce` finishes and final oracle passes.
10. Restore original source and exit `2` when final oracle fails.
11. Record tried, changed, unchanged, and skipped files.

No external `treereduce-rust` binary is called.

## Symlink Policy

Symlinks under `reduce.root` are rejected with exit `2`.

Reason:

- symlinked real packages hide fixture dependencies
- reducer proof needs physical content
- the first implementation uses concrete copied or stubbed content only

## Tree Hashing

The reducer computes a stable hash for `reduce.root`.

Algorithm:

1. Walk with `walkdir`.
2. Reject symlinks and special files.
3. Sort regular files by relative path.
4. Hash each relative path as UTF-8 bytes.
5. Hash a zero byte separator.
6. Hash file bytes.
7. Hash another zero byte separator.
8. Return `sha256:<hex>`.

Use the existing `sha2` dependency.

## Proof Metadata

Normal mode writes JSON to `reduce.proof`.

Dry-run prints the same JSON to stdout and does not write `reduce.proof`.

Shape:

```json
{
  "schema_version": 1,
  "suite": "copied-rust-project",
  "mode": "write",
  "root": "examples/fake-project/reducer-project/project",
  "proof": "examples/fake-project/reducer-project/project/.fixture3-reduce.json",
  "started_at": "2026-05-15T10:00:00Z",
  "finished_at": "2026-05-15T10:00:05Z",
  "baseline_hash": "sha256:...",
  "final_hash": "sha256:...",
  "contract": {
    "exit_code": 0,
    "stdout": {
      "format": "json",
      "require_rows": [
        {
          "rule": "workspace_root",
          "severity": "error"
        }
      ],
      "forbid_rows": [
        {
          "rule": "unexpected_generated_state"
        }
      ]
    },
    "stderr": {
      "forbid_text": ["panicked at"]
    }
  },
  "passes": [
    {
      "name": "generated-dirs",
      "status": "passed",
      "checked": ["target", ".git", "node_modules", "dist"]
    },
    {
      "name": "filetree",
      "status": "changed",
      "tried": ["src/unused.rs"],
      "removed": ["src/unused.rs"],
      "kept": ["src/main.rs"]
    },
    {
      "name": "toml",
      "status": "changed",
      "tried": ["Cargo.toml:dependencies.regex"],
      "removed": ["Cargo.toml:dependencies.regex"],
      "kept": ["Cargo.toml:dependencies.serde_json"]
    },
    {
      "name": "treereduce-rust",
      "status": "changed",
      "tried": ["src/main.rs"],
      "changed": ["src/main.rs"],
      "unchanged": [],
      "skipped": []
    }
  ]
}
```

Proof paths are repository-relative strings.

## Text Output

Normal text output:

```text
suite: copied-rust-project
mode: write
status: changed
filetree_removed: 1
toml_removed: 1
rust_changed: 1
proof: examples/fake-project/reducer-project/project/.fixture3-reduce.json
```

Dry-run text output:

```text
suite: copied-rust-project
mode: dry-run
status: changed
filetree_removed: 1
toml_removed: 1
rust_changed: 1
proof: stdout
```

## Verification Fixture

Add the reducer fixture under:

```text
examples/fake-project/reducer-project/
```

Fixture contents:

```text
examples/fake-project/reducer-project/input.json
examples/fake-project/reducer-project/project/Cargo.toml
examples/fake-project/reducer-project/project/Cargo.lock
examples/fake-project/reducer-project/project/src/main.rs
examples/fake-project/reducer-project/project/src/unused.rs
examples/fake-project/reducer-project/project/README.md
```

The fixture must contain:

- a required Rust file
- a removable Rust file
- a removable Rust function inside the required Rust file
- a removable Cargo dependency
- a required Cargo dependency
- a removable README
- no generated directories in the passing case

Add one generated-directory failure fixture under:

```text
examples/fake-project/reducer-project-generated/
```

That fixture contains:

```text
examples/fake-project/reducer-project-generated/project/target/.keep
```

Expected checks:

- generated-directory fixture exits `2`
- normal reducer fixture exits `0`
- normal reducer fixture removes `src/unused.rs`
- normal reducer fixture removes `README.md`
- normal reducer fixture removes one unused Cargo dependency
- normal reducer fixture changes `src/main.rs`
- normal reducer fixture writes `.fixture3-reduce.json`
- final `fixture3 check --suite reducer-project` exits `0`
- second `fixture3 reduce --suite reducer-project --dry-run` exits `0` and reports zero removed files
- dry-run restores all fixture files

## Documentation

Update `README.md` and top-level `fixture3 --help`.

Required docs:

- `reduce` purpose
- reducer guarantee
- guarantee limits
- manifest `reduce` block
- JSON row contract
- generated directory rejection
- symlink rejection
- filetree pass
- TOML pass
- Rust source pass
- proof metadata
- dry-run behavior
- exit codes
- fake-project reducer workflow

Top-level help must include enough reducer information for an agent to run `reduce` without opening subcommand help.

## Implementation Order

This order is provisional.

Do not execute it until TASK-PLAN-01 replaces it with a research-backed implementation order.

1. Add reducer manifest structs in `manifest.rs`.
2. Add `reduce` args in `args.rs`.
3. Add reducer module skeleton.
4. Add reducer config conversion.
5. Add oracle command execution.
6. Add JSON row matcher.
7. Add tree hash function.
8. Add proof structs and serialization.
9. Add generated-directory pass.
10. Add symlink rejection.
11. Add filetree pass.
12. Add TOML pass.
13. Add Rust source pass.
14. Add fake reducer fixtures.
15. Add reducer verifier script.
16. Wire reducer verifier into `scripts/verify-all.sh`.
17. Update README.
18. Update top-level help.
19. Run full verification.

## Mechanical Verification

Add:

```text
scripts/verify-reducer.sh
```

`scripts/verify-reducer.sh` must verify:

- help text documents `reduce`
- manifest parser accepts `reduce`
- generated directory rejection exits `2`
- filetree pass removes the expected files
- TOML pass removes the expected dependency
- Rust source pass changes the expected Rust file
- proof metadata is written in normal mode
- dry-run writes no proof file and restores files
- final suite check passes after reduction
- second dry-run reduction reports zero removed files

`scripts/verify-all.sh` must run `scripts/verify-reducer.sh`.

Required verification commands before completion:

```bash
cargo fmt --check
cargo check
cargo clippy --all-targets --all-features -- -D warnings
g3rs validate --rules-only
scripts/verify-all.sh
```

Do not add Rust unit tests, integration tests, doc tests, inline `#[test]` functions, or a `tests/` directory.

## Done State

The reducer feature is complete only after:

- all implementation files exist
- fake reducer fixtures exist
- README documents reducer usage
- top-level help documents reducer usage
- `scripts/verify-reducer.sh` passes
- `scripts/verify-all.sh` passes
- proof metadata from the fake reducer fixture shows removed filetree, TOML, and Rust source content
